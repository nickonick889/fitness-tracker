info:
  name: PostTestUsers
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/users/test
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
