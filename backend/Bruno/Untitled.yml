info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/exercise-library/import
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: '{"random": true, "limit": 200, "sourcePoolSize": 200, "pageSize": 25, "maxPages": 20}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
